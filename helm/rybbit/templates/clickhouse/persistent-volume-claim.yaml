apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "rybbit.clickhouse.pvcName" . }}
  labels:
    {{- include "rybbit.clickhouse.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 10Gi
  